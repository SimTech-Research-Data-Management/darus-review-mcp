[project]
name = "darus-review-mcp"
version = "0.1.0"
description = "DaRUS Review MCP Server"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["fastmcp==2.14.5", "pydataverse[mcp]"]

[tool.uv.sources]
pydataverse = { git = "https://github.com/gdcc/pyDataverse.git", branch = "refactor-api" }
